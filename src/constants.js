//how often to update the ball position, if too small can slow the browser down
//if too large ball jumps around too much
export const MOVE_UPDATE_FREQUENCY_SECS = 0.05;

// how much gravity is there! impacts how high the ball goes and how fast it falls
export const GRAVITY_Y_PULL = 2;

// how big is the pinball, its radius in pixels
export const DEFAULT_BALL_SIZE = 8;